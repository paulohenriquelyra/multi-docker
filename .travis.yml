language: generic
sudo: require
services:
    - docker
before.install:
    - docker build -t paulo/react-test -f ./client/Dockerfile.dev ./client
script:
    - docker run -e CI=true paulo/react-test npm test
after_sucess:
    - docker build -t paulolyra/multi-client ./client 
    - docker build -t paulolyra/multi-nginx ./nginx
    - docker build -t paulolyra/multi-server ./server
    - docker build -t paulolyra/multi-worker ./worker
    # docker login
    - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin 
    # take this is image push to docker hub
    - docker push paulolyra/multi-client
    - docker push paulolyra/multi-nginx
    - docker push paulolyra/multi-server
    - docker push paulolyra/multi-worker

